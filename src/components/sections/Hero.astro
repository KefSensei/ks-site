---
import Button from '@/components/ui/Button.astro';
import HeroCanvas from '@/components/ui/HeroCanvas.astro';

// Game scene images — dark collage backdrop (4×3 grid, staggered pulse)
const sceneBg = [
  '/assets/art-samples/SA_1.jpg',
  '/assets/art-samples/FR_main.png',
  '/assets/art-samples/HOG5.jpg',
  '/assets/art-samples/SA_3.jpg',
  '/assets/art-samples/VR_concept1.jpg',
  '/assets/art-samples/HOG6.jpg',
  '/assets/art-samples/FR_c1.png',
  '/assets/art-samples/DOM_store.jpg',
  '/assets/art-samples/HOG12.jpg',
  '/assets/art-samples/SA_5.jpg',
  '/assets/art-samples/VR_bgd_1.jpg',
  '/assets/art-samples/HOG14.jpg',
];

---

<section id="hero" class="relative min-h-[90vh] flex items-center overflow-hidden" style="background: #0D0D0D;">

  <!-- Layer 0: Three.js Particle Canvas -->
  <HeroCanvas />

  <!-- Layer 1: Game scene collage (dark-tinted pulsing grid) -->
  <div class="absolute inset-0 scene-collage" aria-hidden="true">
    {sceneBg.map((src, i) => (
      <div
        class="scene-tile"
        style={`background-image: url('${src}'); animation-delay: ${i * 1.4}s;`}
      ></div>
    ))}
  </div>

  <!-- Layer 2: Dark overlay -->
  <div class="absolute inset-0" style="background: rgba(13,13,13,0.82);"></div>

  <!-- Layer 4: Radial grounding gradient (left side) -->
  <div class="absolute inset-0 pointer-events-none" style="background: radial-gradient(ellipse 60% 90% at 20% 60%, rgba(13,13,13,0.45) 0%, transparent 100%);"></div>

  <!-- Layer 5: Scanlines -->
  <div class="absolute inset-0 pointer-events-none scanlines" aria-hidden="true"></div>

  <!-- Content -->
  <div class="max-w-6xl mx-auto px-6 w-full py-20 relative z-10">
    <div class="max-w-2xl space-y-7">

      <!-- Tag pill -->
      <div id="hero-tag" class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full text-sm font-bold border" style="background: rgba(230,57,70,0.15); border-color: rgba(230,57,70,0.4); color: #FE953A;">
        <span class="w-1.5 h-1.5 rounded-full bg-[#E63946] inline-block animate-pulse"></span>
        Creating FUN since 2007
      </div>

      <!-- Headline -->
      <h1 id="hero-headline" class="text-6xl lg:text-7xl xl:text-8xl font-black leading-[0.9] tracking-tight" style="font-family: var(--font-display);">
        <span id="hero-fun" style="background: linear-gradient(135deg, #E63946 0%, #FE953A 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; display: inline-block;">
          FUN
        </span><br />
        <span id="hero-masters" style="color: #FFFFFF; display: inline-block;">Masters</span>
      </h1>

      <!-- Body copy -->
      <div id="hero-body" class="space-y-4">
        <p class="text-lg leading-relaxed" style="color: rgba(255,255,255,0.72); max-width: 38rem;">
          For nearly two decades, Kef Sensei has been crafting games people genuinely love —
          shipping titles with global publishers, building original IPs, and treating every
          project like it's our own. A tight, senior team obsessed with one mission: deliver
          pure, relentless <strong style="color: #FE953A; font-weight: 700;">FUN</strong>.
        </p>
        <p class="text-base leading-relaxed" style="color: rgba(255,255,255,0.42); max-width: 36rem;">
          Wild concept? Mid-production and need trusted hands? Exploring AI for your pipeline?
          Tell us about your game — the best ones always start with a great conversation.
        </p>
      </div>

      <!-- CTAs -->
      <div id="hero-ctas" class="flex flex-wrap gap-4 pt-1">
        <Button href="/#contact" size="lg" glow>
          Share Your Idea
        </Button>
        <a
          href="/#portfolio"
          class="inline-flex items-center gap-2 px-6 py-3 rounded-full text-base font-bold border border-white/20 text-white/80 hover:border-white/50 hover:text-white transition-all"
        >
          See Our Games
        </a>
      </div>

      <!-- Stats -->
      <div id="hero-stats" class="flex gap-10 pt-5" style="border-top: 1px solid rgba(255,255,255,0.1);">
        {[
          { num: '19+', label: 'Years',          count: 19,   suffix: '+' },
          { num: '15+', label: 'Shipped Titles', count: 15,   suffix: '+' },
          { num: '∞',   label: 'FUN Delivered',  count: null, suffix: ''  },
        ].map(({ num, label, count, suffix }) => (
          <div>
            <div
              class="text-3xl font-black leading-none"
              style="font-family: var(--font-display); background: linear-gradient(135deg, #E63946, #FE953A); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;"
              data-count={count ?? undefined}
              data-suffix={count != null ? suffix : undefined}
            >{num}</div>
            <div class="text-xs font-bold uppercase tracking-wider mt-1" style="color: rgba(255,255,255,0.32);">{label}</div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Bottom fade -->
  <div class="absolute bottom-0 left-0 right-0 h-24 pointer-events-none" style="background: linear-gradient(to bottom, transparent, #F4F4F5);"></div>
</section>

<style>
  /* ── Scene collage grid ───────────────────────────────────── */
  .scene-collage {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    grid-template-rows: repeat(3, 1fr);
  }

  .scene-tile {
    background-size: cover;
    background-position: center;
    animation: scenePulse 9s ease-in-out infinite alternate;
    filter: saturate(0.5) brightness(0.4);
  }

  @keyframes scenePulse {
    0%   { transform: scale(1);    filter: saturate(0.45) brightness(0.38); }
    100% { transform: scale(1.07); filter: saturate(0.7)  brightness(0.52); }
  }

  /* ── Scanlines overlay ────────────────────────────────── */
  .scanlines {
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0,0,0,0.025) 2px,
      rgba(0,0,0,0.025) 4px
    );
    animation: scanMove 80s linear infinite;
    z-index: 5;
    pointer-events: none;
  }

  @keyframes scanMove {
    from { background-position: 0 0; }
    to   { background-position: 0 -240px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .scene-tile { animation: none; }
    .scanlines  { animation: none; }
  }
</style>

<script>
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

gsap.registerPlugin(ScrollTrigger);

const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
if (!reduced) {
  // ── Entrance timeline ──────────────────────────────────
  const tl = gsap.timeline({ defaults: { ease: 'power3.out' } });

  tl.from('#hero-tag',       { scale: 0.7, opacity: 0, duration: 0.5, ease: 'back.out(1.7)' }, 0.15)
    .from('#hero-fun',       { y: 80, opacity: 0, duration: 0.7, ease: 'power4.out' }, 0.35)
    .from('#hero-masters',   { y: 80, opacity: 0, duration: 0.7, ease: 'power4.out' }, 0.52)
    .from('#hero-body > *',  { y: 28, opacity: 0, duration: 0.55, stagger: 0.15 }, 0.88)
    .from('#hero-ctas > *',  { x: -24, opacity: 0, duration: 0.4, stagger: 0.12 }, 1.1)
    .from('#hero-stats > div', { y: 20, opacity: 0, duration: 0.35, stagger: 0.1 }, 1.3);

  // ── Number counters ────────────────────────────────────
  document.querySelectorAll<HTMLElement>('[data-count]').forEach((el) => {
    const end = parseInt(el.dataset.count ?? '0', 10);
    const suffix = el.dataset.suffix ?? '';
    const obj = { val: 0 };
    gsap.to(obj, {
      val: end,
      duration: 1.8,
      delay: 1.5,
      ease: 'power2.out',
      onUpdate() { el.textContent = Math.round(obj.val) + suffix; },
    });
  });

  // ── Periodic glitch on "FUN" ───────────────────────────
  const funEl = document.getElementById('hero-fun');
  function triggerGlitch() {
    if (!funEl) return;
    gsap.timeline()
      .to(funEl, { skewX: 14,  x:  4, duration: 0.04, ease: 'none' })
      .to(funEl, { skewX: -9,  x: -5, duration: 0.04, ease: 'none' })
      .to(funEl, { skewX:  5,  x:  2, duration: 0.04, ease: 'none' })
      .to(funEl, { skewX:  0,  x:  0, duration: 0.12, ease: 'power2.out' });
    setTimeout(triggerGlitch, Math.random() * 7000 + 8000);
  }
  setTimeout(triggerGlitch, 3000);
}
</script>
